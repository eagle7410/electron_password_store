const initialState = {
	list: {
		1: 'All Category',
		2: 'Unknown',
		3: 'First',
		4: 'Sec',
		5: 'Think'
	},
	noChoice : [1]
};

const storageCategories = (state = initialState, action) => {
	// eslint-disable-next-line
	switch (action.type) {
	}

	return state;
};

export {storageCategories};

