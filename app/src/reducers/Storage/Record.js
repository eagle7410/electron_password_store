const initialState = {
	id       : false,
	category : 0,
	title    : '',
	login    : '',
	pass     : '',
	desc     : '',
	answer   : ''
};

const record = (state = initialState, action) => {
	return state;
};

export {record};
