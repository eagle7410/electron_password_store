const initialState = {
	id       : false,
	category : 0,
	title    : '',
	login    : '',
	pass     : '',
	desc     : '',
	answer   : ''
};

const recordAdd = (state = initialState, action) => {
	return state;
};

export {recordAdd};
