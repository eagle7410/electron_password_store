/* eslint-disable */

const initialState = {
	category : 0,
	title    : '',
	login    : false,
	pass     : '',
	desc     : '',
	answer   : ''
};

const add = (state = initialState, action) => {
	return state;
};

export {add};
