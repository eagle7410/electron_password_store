const prefix = 'DropBoxForm';

const DropBoxForm = {
	apiEnter    : `${prefix}EnterData`,
	reconnect   : `${prefix}Reconnect`,
	connectInit : `${prefix}ConnectInit`,
	setToken    : `${prefix}SetToken`
};

export default DropBoxForm;
