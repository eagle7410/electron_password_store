const prefix = 'users';

const Users = {
	init        : `${prefix}Init`,
	create      : `${prefix}Create`,
	createWrite : `${prefix}WriteToCreate`,
	edit        : `${prefix}Edit`,
	editMode    : `${prefix}ModeEdit`,
	editSave    : `${prefix}SaveEdit`,
	editCancel  : `${prefix}CancelEdit`,
	move        : `${prefix}Delete`,
	moveCancel  : `${prefix}CancelDelete`,
};


export default Users;
