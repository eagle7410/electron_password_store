const prefix = 'DropBoxDownloadSteps';

const StepsDownload = {
	run   : `${prefix}Run`,
	next  : `${prefix}Next`,
	stop  : `${prefix}Stop`,
	reset : `${prefix}Reset`
};

export default StepsDownload;
