const maybeNotPermission = 'Maybe not have permissions to create files, not internet, or etc..';
const DropBox = {
	apiEmpty       : 'Enter api data!',
	apiDataBad     : 'Api data no correct or not connect to internet.',
	apiNoAccess    : 'Confirm link bad or old.',
	badConnect     : 'No connect. Maybe not internet, or api data, etc...',
	badTryUpload   : `No upload. ${maybeNotPermission}`,
	badTryDownload : `No download. ${maybeNotPermission}`
};

export default DropBox;
